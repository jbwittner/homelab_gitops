apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Overlay neltharion de l'app whoami : référence la base partagée. Le hostname/domaine
# (whoami.wittnerlab.com) provient de la base ; un autre cluster patcherait ici la
# Certificate / l'IngressRoute pour son propre domaine.
resources:
  - ../../../../components/apps/whoami/base
