# Namespace créé AVANT le CronJob (sync-wave -1). Charge non privilégiée (batch simple) →
# compatible admission PodSecurity `baseline` par défaut, aucun label PSA requis.
apiVersion: v1
kind: Namespace
metadata:
  name: renovate
  annotations:
    argocd.argoproj.io/sync-wave: "-1"
