
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Ordre déclaratif via sync-waves de ressource (pas via l'ordre de cette liste) :
#   namespace (-1) → Job VG (0) → StorageClass (1).
resources:
  - namespace.yaml
#  - vg-bootstrap-job.yaml
#  - storageclass.yaml