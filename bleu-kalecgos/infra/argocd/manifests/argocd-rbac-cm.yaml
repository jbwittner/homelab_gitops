apiVersion: v1
kind: ConfigMap
metadata:
  name: argocd-rbac-cm
  namespace: argocd
data:
  # Défaut : lecture seule. Le groupe authentik "app-argocd-admin" (claim groups)
  # est mappé sur role:admin. Compte local admin conservé en break-glass.
  policy.default: role:readonly
  scopes: '[groups]'
  policy.csv: |
    g, app-argocd-admin, role:admin
