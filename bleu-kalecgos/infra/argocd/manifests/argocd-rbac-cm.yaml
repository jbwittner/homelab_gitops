apiVersion: v1
kind: ConfigMap
metadata:
  name: argocd-rbac-cm
  namespace: argocd
data:
  # Défaut : lecture seule. Le groupe authentik "app-argo-admin" (claim groups)
  # est mappé sur role:admin. Compte local admin conservé en break-glass.
  policy.default: role:readonly
  scopes: '[groups]'
  policy.csv: |
    g, app-argo-admin, role:admin
