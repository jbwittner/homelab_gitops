apiVersion: cilium.io/v2alpha1
kind: CiliumL2AnnouncementPolicy
metadata:
  name: bleu-kalecgos-l2
spec:
  loadBalancerIPs: true
  # Pour restreindre à l'interface physique de vert-eranikus :
  # interfaces:
  #   - ^enp1s0$
  nodeSelector:
    matchLabels:
      kubernetes.io/os: linux