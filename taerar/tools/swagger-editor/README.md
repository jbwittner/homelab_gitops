# Swagger Editor

Éditeur OpenAPI/Swagger en ligne (`swaggerapi/swagger-editor`, variante `unprivileged`) —
conteneur `swagger-editor`.

## Détails

- Conteneur unique, sans état, sans `.env`.
- Port interne `8080` ; exposé via le reverse-proxy Dokploy.
