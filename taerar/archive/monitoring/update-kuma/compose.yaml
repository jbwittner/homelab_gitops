services:
  uptime-kuma:
    image: louislam/uptime-kuma:2.3.2
    container_name: monitoring_uptime_kuma
    restart: always
    volumes:
      - uptime-kuma-data:/app/data
      - /var/run/docker.sock:/var/run/docker.sock

volumes:
  uptime-kuma-data:
