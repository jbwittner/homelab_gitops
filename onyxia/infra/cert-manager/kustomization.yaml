apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Config post-install spécifique au cluster (rendue par la 2ᵉ source de l'Application) :
# les deux ClusterIssuers Let's Encrypt (prod + staging) et le token Cloudflare scellé.
resources:
  - cluster-issuer-prod.yaml
  - cluster-issuer-staging.yaml
  # ⚠️ TODO onyxia : le token Cloudflare doit être scellé contre le contrôleur sealed-secrets
  # d'onyxia (celui de neltharion NE marche PAS ici). Procédure : README.md de ce dossier.
  # Décommenter une fois cloudflare-api-token.sealed-secret.yaml généré et commité.
  # - cloudflare-api-token.sealed-secret.yaml
