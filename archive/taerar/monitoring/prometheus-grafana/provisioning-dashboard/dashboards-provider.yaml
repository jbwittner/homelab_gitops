apiVersion: 1

providers:
  - name: 'default'
    orgId: 1
    folder: ''
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    options:
      # The path inside the Grafana container where the JSON files will be located
      path: /var/lib/grafana/dashboards