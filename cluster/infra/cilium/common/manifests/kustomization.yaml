apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Base commune, référencée par le `manifests/` de chaque cluster. Ce dossier n'est jamais
# déployé seul : il est exclu du generator git de `cilium.appset.yaml`.
resources:
  - l2-policy.yaml
