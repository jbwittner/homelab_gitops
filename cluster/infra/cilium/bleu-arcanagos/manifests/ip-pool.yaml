apiVersion: cilium.io/v2
kind: CiliumLoadBalancerIPPool
metadata:
  # Préfixé par le `namePrefix` du kustomization.yaml → `bleu-arcanagos-pool`.
  name: pool
spec:
  blocks:
    - start: "192.168.1.85"
      stop: "192.168.1.89"