apiVersion: cilium.io/v2
kind: CiliumLoadBalancerIPPool
metadata:
  # Préfixé par le `namePrefix` du kustomization.yaml → `bleu-kalecgos-pool`.
  name: pool
spec:
  blocks:
    - start: "192.168.1.80"
      stop: "192.168.1.89"